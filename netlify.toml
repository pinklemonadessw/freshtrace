[build]
command = "flutter build web --release"
publish = "build/web"

[[plugins]]
package = "netlify-plugin-flutter"

  [plugins.inputs]
  channel = "stable"

# Flutter web is a single-page app: route everything to index.html
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
