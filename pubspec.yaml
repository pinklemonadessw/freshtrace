name: grocery_manager_alpha
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.6.0
  cloud_firestore: 6.1.2
  firebase_auth: 6.1.4
  mobile_scanner: ^7.1.4
  http: ^1.6.0
  intl: ^0.20.2
  firebase_ai: ^3.8.0
  flutter_markdown: ^0.7.7+1
  cloud_functions: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
